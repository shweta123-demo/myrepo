pipeline {
    agent any
    stages {
        stage('clonestage'){
            steps {
                git 'https://github.com/shweta123-demo/myrepo.git'
            }
        }
        stage('buildpackage'){
            steps {
                sh 'mvn clean package'
            }
        }
        stage ('release'){
            steps {
                archive 'target/*.jar'
            }
        }
    }
}
